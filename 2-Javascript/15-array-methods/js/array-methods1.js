let arr = [1, 8, 2, -5];
// push will add a new element to the END of the array
 //arr.push(7);

// unshift will add a new element to the beginning of the array
//arr.unshift(7);

// pop will delete the last element in the Array
// const x = arr.pop();

// shift will delete the first element in the Array
// const x = arr.shift();

// reverse will reverse the order of the array elements
//arr.reverse();

// toString will RETURN a string text of the array elements
// separated with commas
// const x = arr.toString();

// join will RETURN a string text of the array elements 
// separated with a specified string
const x = arr.join('|');

console.log(arr);
console.log(x);